[build]
  command = "npm run build"
  publish = "build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Optional: uncomment to force npm to ignore peer deps during Netlify installs
# [build.environment]
#   NPM_FLAGS = "--legacy-peer-deps"
